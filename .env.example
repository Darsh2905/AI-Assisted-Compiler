# Copy this file to .env and fill in your real key. .env is git-ignored;
# .env.example is safe to commit because it holds no secret.
#
#   cp .env.example .env
#   # then edit .env and paste your key in
#
GROQ_API_KEY=
GROQ_MODEL=openai/gpt-oss-120b
